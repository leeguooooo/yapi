import utils from './utils.cjs';

export default utils;

export const {
  handleJson,
  handleParamsValue,
  simpleJsonPathParse,
  handleMockWord,
  joinPath,
  safeArray,
  isJson5,
  isJson,
  unbase64,
  json_parse,
  json_format,
  ArrayToObject,
  timeago,
  schemaValidator
} = utils;
